{
  "name": "career-ops-studio",
  "version": "1.0.0",
  "description": "Desktop shell for career-ops — workspace, job browser and terminal in one window. Installs the career-ops workspace it runs on.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "bootstrap": "node scripts/bootstrap.mjs",
    "overlay": "node scripts/bootstrap.mjs",
    "postinstall": "node scripts/bootstrap.mjs"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0",
    "js-yaml": "^5.4.1",
    "node-pty": "^1.1.0",
    "ws": "^8.21.3"
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "get": "bin/get"
  }
}
